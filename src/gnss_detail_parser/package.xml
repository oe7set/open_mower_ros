<?xml version="1.0"?>
<package format="2">
  <name>gnss_detail_parser</name>
  <version>0.1.0</version>
  <description>
    Parses the raw NMEA/UBX stream from the v2 firmware GPS debug interface
    (TCP) and publishes detailed GNSS diagnostics (xbot_msgs/GnssDetail) for the
    GNSS page. Keeps the heavy per-satellite / RF parsing off the firmware's
    real-time thread; the firmware only emits the navigation-critical outputs.
  </description>

  <maintainer email="dev@openmower.local">OpenMower</maintainer>
  <license>GPL-3.0</license>

  <buildtool_depend>catkin</buildtool_depend>

  <depend>roscpp</depend>
  <depend>xbot_msgs</depend>

  <exec_depend>roslaunch</exec_depend>
</package>
